/**
 * create by <%= author %>
 */

import React from 'react'

const <%= Ab %> = React.createClass({
    render() {
        return (
            <div>
                <%= u_name %> <%= ab %> view
            </div>

        )
    }

})

export default <%= Ab %>
