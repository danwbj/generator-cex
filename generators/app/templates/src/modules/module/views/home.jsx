import React from 'react'

const Home = React.createClass({
    render() {
        return (
            <div>
                <%= u_name %> home view
            </div>

        )
    }

})

export default Home
