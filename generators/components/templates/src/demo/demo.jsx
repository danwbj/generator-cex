/**
 * create by <%= author %>
 */

import React from 'react'
import classNames from 'classnames'

import './<%= ab %>.less'

const <%= Ab %> = React.createClass({
    render() {
        return (
            <div>
               <%= ab %> component
            </div>

        )
    }

})

export default <%= Ab %>
